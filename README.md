# Дашборд IT-компаний Калининградской области

Веб-приложение для анализа и визуализации данных об IT-компаниях Калининградской области. Система позволяет просматривать информацию о компаниях, фильтровать данные, проверять аккредитацию и создавать отчеты.

## 🚀 Основные возможности

- **Просмотр данных**: Анализ 120+ IT-компаний с информацией о выручке, налогах, численности сотрудников
- **Фильтрация**: Множественные фильтры по различным параметрам (ОКВЭД, выручка, налоги, численность, аккредитация)
- **Проверка аккредитации**: Автоматическая проверка статуса аккредитации через API Госуслуг
- **Система ролей**: Разделение доступа между директором и сотрудником
- **Генерация отчетов**: Создание детальных отчетов по выбранным компаниям (только для директора)
- **Интерактивный интерфейс**: Живой поиск, сортировка таблиц, быстрые фильтры

## 🛠 Технологии

- **Backend**: Django 5.2.8 (Python)
- **Frontend**: HTML, CSS, JavaScript (vanilla)
- **База данных**: SQLite
- **Внешние API**: Госуслуги (проверка аккредитации IT-компаний)

## 📋 Требования

- Python 3.8+
- pip (менеджер пакетов Python)

## 🔧 Установка

1. **Клонируйте репозиторий** (или скачайте проект):
   ```bash
   git clone <url-репозитория>
   cd my_prof
   ```

2. **Создайте виртуальное окружение** (рекомендуется):
   ```bash
   python -m venv venv
   ```

3. **Активируйте виртуальное окружение**:
   - Windows:
     ```bash
     venv\Scripts\activate
     ```
   - Linux/Mac:
     ```bash
     source venv/bin/activate
     ```

4. **Установите зависимости**:
   ```bash
   pip install -r requirements.txt
   ```

5. **Примените миграции базы данных**:
   ```bash
   python manage.py migrate
   ```

6. **Создайте тестовых пользователей**:
   ```bash
   python manage.py create_users
   ```

## 🏃 Запуск проекта

### Вариант 1: Использование bat-файла (Windows)
```bash
run_dashboard.bat
```

### Вариант 2: Запуск через manage.py
```bash
python manage.py runserver
```

После запуска откройте браузер и перейдите по адресу: **http://127.0.0.1:8000/**

## 👤 Учетные данные для входа

После выполнения команды `create_users` будут созданы следующие пользователи:

### Директор (полный доступ):
- **Логин**: `director`
- **Пароль**: `director123`

### Сотрудник (ограниченный доступ):
- **Логин**: `employee`
- **Пароль**: `employee123`

## 📁 Структура проекта

```
my_prof/
├── dashboard_app/            # Основное приложение
│   ├── migrations/           # Миграции базы данных
│   ├── management/           # Кастомные команды (create_users)
│   ├── services.py           # Бизнес-логика и работа с данными
│   └── ...                   # Остальные файлы приложения
├── dashboard_site/           # Настройки проекта Django
│   ├── settings.py           # Конфигурация приложения
│   └── urls.py               # Маршрутизация URL
├── data/
│   └── dop_material.csv      # Исходные данные о компаниях
├── static/                   # Статические файлы (CSS + JS)
│   └── dashboard_app/
├── templates/                # HTML шаблоны
│   └── dashboard_app/
├── manage.py                 # Точка входа Django
├── requirements.txt          # Зависимости проекта
├── run_dashboard.bat         # Быстрый запуск на Windows
└── README.md                 # Этот файл
```

## 🔐 Система ролей

### Директор
- ✅ Просмотр всех данных
- ✅ Использование всех фильтров
- ✅ Обновление аккредитации компаний
- ✅ Создание отчетов

### Сотрудник
- ✅ Просмотр всех данных
- ✅ Использование всех фильтров
- ❌ Обновление аккредитации (недоступно)
- ❌ Создание отчетов (недоступно)

## 📊 Функционал

### Фильтры
- Поиск по названию компании или ФИО руководителя
- Фильтр по ОКВЭД
- Фильтр по применению УСН
- Фильтр по аккредитации
- Фильтры по выручке (мин/макс)
- Фильтр по налогам
- Фильтр по численности сотрудников
- Фильтры по годам данных

### Статистика
- Совокупная выручка
- Совокупные налоги
- Средняя численность штата
- Доля компаний на УСН
- Топ компания по выручке

### Аккредитация
Система автоматически проверяет статус аккредитации компаний через API Госуслуг. Директор может обновить статус аккредитации для выбранных компаний.

## 🛠 Управление через админ-панель

Доступ к админ-панели: http://127.0.0.1:8000/admin/

Для входа используйте суперпользователя (создайте через `python manage.py createsuperuser`).

## 📝 Команды управления

```bash
# Применить миграции
python manage.py migrate

# Создать суперпользователя
python manage.py createsuperuser

# Создать тестовых пользователей
python manage.py create_users

# Запустить сервер разработки
python manage.py runserver

# Собрать статические файлы (для продакшена)
python manage.py collectstatic
```

## 🔄 Обновление данных

Данные о компаниях загружаются из файла `data/dop_material.csv`. Для обновления данных:
1. Замените файл `data/dop_material.csv` новыми данными
2. Перезапустите сервер (данные кэшируются в памяти)

## 🐛 Решение проблем

### Ошибка при миграциях
```bash
python manage.py migrate --run-syncdb
```

### Ошибка импорта модулей
Убедитесь, что виртуальное окружение активировано и все зависимости установлены:
```bash
pip install -r requirements.txt
```

### Проблемы с кодировкой (Windows)
Используйте флаг `-X utf8` при запуске Python:
```bash
python -X utf8 manage.py <команда>
```

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 👨‍💻 Автор

Разработано для анализа данных IT-компаний Калининградской области.

## 📞 Поддержка

При возникновении проблем создайте issue в репозитории проекта.

